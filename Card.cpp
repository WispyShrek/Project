/**
 * @file Card.cpp
 * @brief Implementation of the Card class.
 */
#include "Card.h"

/**
 * @brief Executes the payment process for a card.
 *
 * This method implements the payment logic for a card transaction.
 * It prints a message to the standard output to signify that the payment
 * is being processed via card.
 */
void Card::pay() {
	std::cout << "Pay with card"; 
}
